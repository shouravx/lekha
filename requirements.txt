# Lekha — Python dependencies
# Tested on Windows 10/11, Python 3.10-3.12, Intel i3-10100, 16GB RAM, no dedicated GPU.
# Install with: pip install -r requirements.txt

# --- UI ---------------------------------------------------------------
streamlit==1.40.2

# --- PDF handling -------------------------------------------------------
PyMuPDF==1.24.14

# --- Translation engine (fully offline after model download) -----------
argostranslate==1.11.0
sentencepiece==0.2.0

# --- Output document generation -----------------------------------------
python-docx==1.1.2
reportlab==4.2.5

# --- Hybrid pipeline (optional — both stages are opt-in) ---------------
# Online translation backend. deep-translator is chosen over googletrans
# because it is actively maintained and also fronts DeepL/MyMemory/
# LibreTranslate, so switching providers is a constructor change.
# Only needed if you use the Google Translate backend.
deep-translator==1.11.4

# HTTP client for the local Ollama refinement stage. Only needed if you
# use the "polish with a local AI model" option. The model itself is not
# a Python dependency — install Ollama separately and run:
#     ollama pull qwen2.5:3b
requests==2.32.3

# --- OCR (optional — only needed if OCR is enabled in Settings) --------
# PaddleOCR pulls in paddlepaddle, which is a large download (several
# hundred MB). If you don't need OCR for scanned PDFs, you can skip
# these two lines and OCR will simply stay greyed out in Settings.
paddleocr==3.0.3
paddlepaddle==3.0.0

# --- Imaging (used by both PDF rendering and OCR) -----------------------
Pillow==10.4.0
numpy==1.26.4
